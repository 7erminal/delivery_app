import 'package:flutter/material.dart';

class RetailerItem extends StatefulWidget {
  const RetailerItem({Key? key}) : super(key: key);

  @override
  State<RetailerItem> createState() => _RetailerItemState();
}

class _RetailerItemState extends State<RetailerItem> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
